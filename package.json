{
  "name": "dofustouch-emu",
  "description": "Emulateur dofus touch",
  "version": "1.0.0",
  "private": true,
  "author": "Rémi Caumette <remi@caumette.me>",
  "main": "./main.js",
  "scripts": {
    "start": "electron .",
    "dist-mac": "build --mac",
    "dist-win": "docker run --rm -it -v `pwd`:/project electronuserland/electron-builder:wine /bin/bash -c 'npm install && ./node_modules/.bin/build --win'",
    "dist-linux": "docker run --rm -it -v `pwd`:/project electronuserland/electron-builder:wine /bin/bash -c 'npm install && ./node_modules/.bin/build --linux'"
  },
  "build": {
    "appId": "me.caumette.remi.dofustouchemu",
    "productName": "DofusTouchEmu"
  },
  "dependencies": {
    "browser-es-module-loader": "^0.2.0",
    "jquery": "^3.1.1"
  },
  "devDependencies": {
    "electron": "^1.4.13",
    "electron-builder": "next"
  }
}
